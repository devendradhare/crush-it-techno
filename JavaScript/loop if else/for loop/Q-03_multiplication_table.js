// 3 Multiplication Table:
// Write a program to print the multiplication table of a given integer
// up to a specified range using nested for loops.
// Input: An integer and the range (both integers).

let range = 20;

for (let i = 1; i <= range; i++) {
  console.log(range, " * ", i, " = ", range * i);
}
